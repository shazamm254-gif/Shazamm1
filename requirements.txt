# Core dependency for talking to the YouTube Data API v3 (REST, no SDK needed).
requests>=2.31.0

# Required for the personal agent (tools/agent.py), which uses the SDK's
# tool-calling loop. Also powers the --use-claude flags on the other tools --
# those fall back to built-in rule-based logic without it.
anthropic>=1.0.0
